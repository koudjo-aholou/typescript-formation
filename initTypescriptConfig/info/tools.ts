// Typescript error

// Limit the use of any Type : Tyoescript s type system is gradual (add  types little by little) and optionnal because you can disable the type checker
// strick null check

// const y: number | null = null;

// let yo: number = undefined;
// implicit any
// const yy = (a, b) => a + b;

// const notAny: any = 'y';
