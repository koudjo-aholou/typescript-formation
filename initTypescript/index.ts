function greeter243(person) {
  return "Hello, " + person;
}

// Déclaration d'une variable avec typage explicite
let message42: string = "Bonjour TypeScript !";

// Fonction avec typage explicite des paramètres et du retour
function add11(x: number, y: number): number {
    return x + y;
}